class Assignment {
    constructor(course, type, dueDate, dueTime, description, name) {
        var course = course
        var type = type
        var dueDate = dueDate
        var dueTime = dueTime
        var description = description
        var name = name
    }
}